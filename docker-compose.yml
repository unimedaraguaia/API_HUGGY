services:
  api:
    container_name: api_huggy_tst
    build: ./api
    ports:
      - 3000:3000
    restart: always
    env_file:
      - ./api/.env
    
  nginx:
    container_name: nginx_api
    build: ./nginx
    ports:
      - 80:80

